import './main.jsx';

// This file is a small shim to satisfy any cached requests
// (some browsers or service workers may still request `main.tsx`).
// It simply imports the actual `main.jsx` entry so dev server can serve it.
export {};
